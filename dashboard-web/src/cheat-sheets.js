// Cheat sheet data — keyed by company name. When an entry exists, the drawer
// renders the rich tabbed view; otherwise it falls back to the standard report
// view. Ships empty so no personal evaluation data is bundled — populate per
// deployment, or rely on the report-derived cheat sheet (v1ToCheatsheet).
window.CHEAT_SHEETS = {};
